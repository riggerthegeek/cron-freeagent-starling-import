{
  "name": "cron-freeagent-starling-import",
  "version": "0.2.1",
  "description": "Export Starling transactions into FreeAgent",
  "main": "import.js",
  "scripts": {
    "postversion": "git push --tags && git push",
    "test": "npm run -s test:lint",
    "test:lint": "semistandard"
  },
  "author": "Simon Emms <simonemms.com>",
  "license": "MIT",
  "dependencies": {
    "ms": "^2.1.1",
    "nodemailer": "^4.6.3",
    "nodemailer-mailgun-transport": "^1.3.6",
    "request": "^2.85.0",
    "request-promise-native": "^1.0.5"
  },
  "devDependencies": {
    "semistandard": "^12.0.1"
  }
}
